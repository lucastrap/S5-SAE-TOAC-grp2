<!DOCTYPE html>

<title>Post</title>

{% block body %}
    <h1>Post</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ post.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ post.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dashboard_blog_index') }}">back to list</a>

    <a href="{{ path('app_dashboard_blog_edit', {'id': post.id}) }}">edit</a>

    {{ include('dashboard/blog/_delete_form.html.twig') }}
{% endblock %}
