{% extends 'base.html.twig' %}

{% block title %}Evenement{% endblock %}

{% block body %}
    <h1>Evenement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evenement.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ evenement.title }}</td>
            </tr>
            <tr>
                <th>Start</th>
                <td>{{ evenement.start }}</td>
            </tr>
            <tr>
                <th>ToDate</th>
                <td>{{ evenement.toDate ? evenement.toDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dashboard_evenement_index') }}">back to list</a>

    <a href="{{ path('app_dashboard_evenement_edit', {'id': evenement.id}) }}">edit</a>

    {{ include('dashboard/evenement/_delete_form.html.twig') }}
{% endblock %}
