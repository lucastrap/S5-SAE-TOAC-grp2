{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="text-center w-min bg-green-700 hover:bg-green-900 text-white font-bold py-2 px-4 rounded-full">{{ button_label|default('Valider') }}</button>
{{ form_end(form) }}
